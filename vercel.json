{
  "version": 2,
  "builds": [
    {
      "src": "packages/server/bin/*",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "pnpm install && pnpm run build",
  "installCommand": "pnpm install",
  "env": {
    "NODE_ENV": "production",
    "NODE_VERSION": "20"
  }
}
